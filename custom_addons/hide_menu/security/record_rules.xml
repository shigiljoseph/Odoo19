<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="hide_selected_record_rules" model="ir.rule">
        <field name="name">Hide selected field</field>
        <field name="model_id" ref="model_ir_ui_menu"/>
        <field name="domain_force">[('id', 'not in', user.menus_ids)]</field>
        <field name="groups" eval="[(Command.link(ref('	sales_team.group_sale_manager')))]"/>
    </record>

</odoo>